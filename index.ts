(async () => {  const fs = await import('fs/promises'); for (const mod of Array.from({ length: 13 }, (_, i) => i + 1)) { try { Promise.all([ fs.mkdir(`module-${mod}`),  fs.mkdir(`./module-${mod}/classwork`),  fs.mkdir(`./module-${mod}/homework`), fs.writeFile(`./module-${mod}/README.md`, `# Module ${mod}\n\n## Classwork\n\n## Homework\n`),  fs.writeFile(`./module-${mod}/classwork/README.md`, `# Classwork\n`), fs.writeFile(`./module-${mod}/homework/README.md`, `# Homework\n`) ]) } catch (error) { console.error(error); } finally { console.log(`Directory module-${mod} created successfully`); } } })(); 